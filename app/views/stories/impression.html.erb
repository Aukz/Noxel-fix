  <div class="text-center">
    <h1><%= @story.title %></h1>
  </div>
  <div class="row">

  <div class="ml-5 mt-5">
     <%= link_to "感想を書く",new_impression_path(story_id: @story.id), class: "btn btn-secondary" %>
  </div>

  <div class="mx-auto card p-3 border-0 w-75 mt-4">
    <%= render @impressions %>
  </div>

</div>
