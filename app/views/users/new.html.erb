<% provide(:button_text, "アカウント作成") %>

<div class="row mt-5">
  <div class="col-md-8 mx-auto">
    <h3>新規登録</h3>

    <%= render 'form', user: @user %>
    <%= link_to 'Back', root_path %>
  </div>
</div>
